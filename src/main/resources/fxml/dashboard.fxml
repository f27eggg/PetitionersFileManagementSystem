<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.chart.*?>

<!--
  仪表盘页面
  功能：显示系统概览、统计数据卡片
  @author 刘一村
  @version 1.0.0
-->
<VBox xmlns="http://javafx.com/javafx/17"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.petition.controller.DashboardController"
      spacing="20.0" styleClass="dashboard-container">

    <padding>
        <Insets top="10.0" right="10.0" bottom="10.0" left="10.0"/>
    </padding>

    <!-- 页面标题 -->
    <Label text="📊 数据仪表盘" styleClass="page-title"/>

    <!-- 统计卡片区域 -->
    <GridPane hgap="20.0" vgap="20.0">
        <!-- 第一行卡片 -->
        <!-- 总人数卡片 -->
        <VBox styleClass="stat-card" GridPane.columnIndex="0" GridPane.rowIndex="0">
            <padding>
                <Insets top="20.0" right="20.0" bottom="20.0" left="20.0"/>
            </padding>
            <Label text="总人数" styleClass="stat-label"/>
            <Label fx:id="totalCountLabel" text="0" styleClass="stat-value"/>
            <Label text="所有上访人员" styleClass="stat-desc"/>
        </VBox>

        <!-- 高危人员卡片 -->
        <VBox styleClass="stat-card, stat-card-danger" GridPane.columnIndex="1" GridPane.rowIndex="0">
            <padding>
                <Insets top="20.0" right="20.0" bottom="20.0" left="20.0"/>
            </padding>
            <Label text="高危人员" styleClass="stat-label"/>
            <Label fx:id="highRiskCountLabel" text="0" styleClass="stat-value"/>
            <Label text="危险等级：高" styleClass="stat-desc"/>
        </VBox>

        <!-- 中危人员卡片 -->
        <VBox styleClass="stat-card, stat-card-warning" GridPane.columnIndex="2" GridPane.rowIndex="0">
            <padding>
                <Insets top="20.0" right="20.0" bottom="20.0" left="20.0"/>
            </padding>
            <Label text="中危人员" styleClass="stat-label"/>
            <Label fx:id="mediumRiskCountLabel" text="0" styleClass="stat-value"/>
            <Label text="危险等级：中" styleClass="stat-desc"/>
        </VBox>

        <!-- 低危人员卡片 -->
        <VBox styleClass="stat-card, stat-card-success" GridPane.columnIndex="3" GridPane.rowIndex="0">
            <padding>
                <Insets top="20.0" right="20.0" bottom="20.0" left="20.0"/>
            </padding>
            <Label text="低危人员" styleClass="stat-label"/>
            <Label fx:id="lowRiskCountLabel" text="0" styleClass="stat-value"/>
            <Label text="危险等级：低" styleClass="stat-desc"/>
        </VBox>

        <!-- 列宽约束 -->
        <columnConstraints>
            <ColumnConstraints percentWidth="25.0"/>
            <ColumnConstraints percentWidth="25.0"/>
            <ColumnConstraints percentWidth="25.0"/>
            <ColumnConstraints percentWidth="25.0"/>
        </columnConstraints>
    </GridPane>

    <!-- 快速操作区域 -->
    <HBox spacing="15.0" alignment="CENTER_LEFT">
        <Button text="➕ 新增人员" styleClass="primary-button" onAction="#addPetitioner"/>
        <Button text="📥 导入数据" styleClass="secondary-button" onAction="#importData"/>
        <Button text="📤 导出数据" styleClass="secondary-button" onAction="#exportData"/>
        <Button text="🔄 刷新数据" styleClass="secondary-button" onAction="#refreshData"/>
    </HBox>

    <!-- 图表区域 -->
    <HBox spacing="20.0" VBox.vgrow="ALWAYS">
        <!-- 危险等级分布饼图 -->
        <VBox styleClass="chart-container" HBox.hgrow="ALWAYS">
            <Label text="危险等级分布" styleClass="chart-title"/>
            <PieChart fx:id="riskLevelChart" legendVisible="true" labelsVisible="true"
                      prefHeight="300" VBox.vgrow="ALWAYS"/>
        </VBox>

        <!-- 上访次数分布柱状图 -->
        <VBox styleClass="chart-container" HBox.hgrow="ALWAYS">
            <Label text="上访次数分布" styleClass="chart-title"/>
            <BarChart fx:id="visitCountChart" prefHeight="300" VBox.vgrow="ALWAYS">
                <xAxis>
                    <CategoryAxis label="次数范围"/>
                </xAxis>
                <yAxis>
                    <NumberAxis label="人数"/>
                </yAxis>
            </BarChart>
        </VBox>

        <!-- 籍贯分布柱状图 -->
        <VBox styleClass="chart-container" HBox.hgrow="ALWAYS">
            <Label text="籍贯分布（前10）" styleClass="chart-title"/>
            <BarChart fx:id="nativePlaceChart" prefHeight="300" VBox.vgrow="ALWAYS">
                <xAxis>
                    <CategoryAxis label="籍贯"/>
                </xAxis>
                <yAxis>
                    <NumberAxis label="人数"/>
                </yAxis>
            </BarChart>
        </VBox>
    </HBox>

</VBox>
