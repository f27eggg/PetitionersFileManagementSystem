<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.image.Image?>
<?import javafx.scene.text.Text?>

<!--
  主窗口布局文件
  功能：应用程序主框架，包含顶部标题栏、左侧导航栏、中央内容区域
  @author 刘一村
  @version 1.0.0
-->
<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.petition.controller.MainController"
            prefHeight="900.0" prefWidth="1600.0"
            styleClass="main-container">

    <!-- 顶部：标题栏 -->
    <top>
        <HBox styleClass="header" alignment="CENTER_LEFT" spacing="15.0">
            <padding>
                <Insets top="15.0" right="20.0" bottom="15.0" left="20.0"/>
            </padding>

            <!-- 菜单按钮（控制侧边栏展开/收起） -->
            <Button fx:id="menuButton" styleClass="menu-button" text="☰"
                    onAction="#toggleSidebar"/>

            <!-- Logo和标题区域 -->
            <HBox alignment="CENTER_LEFT" spacing="15.0" HBox.hgrow="ALWAYS">
                <!-- 警徽图标 -->
                <ImageView fx:id="logoImage" fitHeight="50.0" fitWidth="50.0" preserveRatio="true"/>

                <!-- 系统标题 -->
                <VBox alignment="CENTER_LEFT">
                    <Label text="济南市公安局历下分局刑侦大队" styleClass="header-title"/>
                    <Label text="上访人员重点监控信息管理系统" styleClass="header-subtitle"/>
                </VBox>
            </HBox>

            <!-- 右侧功能区 -->
            <HBox alignment="CENTER_RIGHT" spacing="10.0">
                <!-- 当前用户 -->
                <Label fx:id="currentUserLabel" text="管理员" styleClass="user-label"/>

                <!-- 退出按钮 -->
                <Button text="退出" styleClass="logout-button" onAction="#logout"/>
            </HBox>
        </HBox>
    </top>

    <!-- 左侧：导航栏 -->
    <left>
        <VBox fx:id="sidebar" styleClass="sidebar" prefWidth="250.0">
            <padding>
                <Insets top="10.0" right="0.0" bottom="10.0" left="0.0"/>
            </padding>

            <!-- 导航菜单 -->
            <VBox fx:id="navMenu" styleClass="nav-menu" spacing="5.0">
                <!-- 仪表盘 -->
                <Button fx:id="navDashboard" text="📊 仪表盘"
                        styleClass="nav-button, nav-button-active"
                        onAction="#showDashboard" maxWidth="Infinity"/>

                <!-- 人员管理 -->
                <Button fx:id="navPetitioners" text="👥 人员管理"
                        styleClass="nav-button"
                        onAction="#showPetitioners" maxWidth="Infinity"/>

                <!-- 高级查询 -->
                <Button fx:id="navQuery" text="🔍 高级查询"
                        styleClass="nav-button"
                        onAction="#showQuery" maxWidth="Infinity"/>

                <!-- 统计分析 -->
                <Button fx:id="navStatistics" text="📈 统计分析"
                        styleClass="nav-button"
                        onAction="#showStatistics" maxWidth="Infinity"/>

                <!-- 数据管理 -->
                <Button fx:id="navData" text="💾 数据管理"
                        styleClass="nav-button"
                        onAction="#showData" maxWidth="Infinity"/>

                <!-- 系统设置 -->
                <Button fx:id="navSettings" text="⚙️ 系统设置"
                        styleClass="nav-button"
                        onAction="#showSettings" maxWidth="Infinity"/>
            </VBox>

            <!-- 底部信息 -->
            <VBox styleClass="sidebar-footer" alignment="BOTTOM_LEFT" VBox.vgrow="ALWAYS">
                <padding>
                    <Insets top="20.0" right="15.0" bottom="10.0" left="15.0"/>
                </padding>
                <Label fx:id="versionLabel" text="版本 v1.0.0" styleClass="version-label"/>
                <Label fx:id="recordCountLabel" text="记录数：0" styleClass="info-label"/>
            </VBox>
        </VBox>
    </left>

    <!-- 中央：内容区域 -->
    <center>
        <StackPane fx:id="contentArea" styleClass="content-area">
            <!-- 动态加载的页面内容将显示在这里 -->
        </StackPane>
    </center>

</BorderPane>
